{
    "name": "collaboard",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev" : "pnpm --filter web dev && pnpm --filter server dev",
        "build": "pnpm -r build",
        "test": "pnpm -r test",
        "lint": "pnpm -r lint"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "typescript": "^5.3.3"
      }
}